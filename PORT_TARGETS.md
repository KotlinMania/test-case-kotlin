# Port targets — `test-case-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `test-case` that codex-rs uses, and therefore the minimum subset that `test-case-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-150] (rank `0`, tier `direct`, status `To Do`)

**Cargo package name**: `test-case`
**Rust module name (in `use` statements)**: `test_case`

## Summary

- **1** distinct symbols imported by codex-rs
- **6** total `use` statements
- **6** codex source files affected
- **2** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `test_case::test_case`

- **6 `use` site(s)** across **2 codex member crate(s)**: `codex-core`, `codex-exec-server`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `core/tests/suite/apply_patch_cli.rs:42` (`codex-core`) — `use test_case::test_case`
- `core/tests/suite/approvals.rs:51` (`codex-core`) — `use test_case::test_case`
- `core/tests/suite/shell_command.rs:17` (`codex-core`) — `use test_case::test_case`
- `core/tests/suite/shell_serialization.rs:25` (`codex-core`) — `use test_case::test_case`
- `exec-server/tests/exec_process.rs:21` (`codex-exec-server`) — `use test_case::test_case`
- `exec-server/tests/file_system.rs:37` (`codex-exec-server`) — `use test_case::test_case`
